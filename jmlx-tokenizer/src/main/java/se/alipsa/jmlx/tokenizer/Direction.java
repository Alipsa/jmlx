package se.alipsa.jmlx.tokenizer;

/** Direction used when truncating or padding a single token sequence. */
public enum Direction {
  /** Remove or add entries at the beginning. */
  LEFT,
  /** Remove or add entries at the end. */
  RIGHT
}
