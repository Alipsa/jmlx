package se.alipsa.jmlx.tokenizer;

/**
 * A {@code {"Sequence": {"id": "A"}}}} template item: splice in the real encoded token sequence
 * here.
 */
public record SequenceItem() implements TemplateItem {}
